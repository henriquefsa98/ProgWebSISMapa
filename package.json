{
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "name": "progwebsismapa",
  "description": "Esse template está configurado para criar um ambiente com as principais dependências que precisamos na aula. Para criar um novo projeto no ambiente:",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "npx htmlhint static/*.html && npx stylelint static/*.css && npx eslint src/*.ts",
    "watch": "npx webpack -w"
  },
  "author": "",
  "license": "ISC"
}
